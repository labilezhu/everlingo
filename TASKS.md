# Tasks

## 计划的任务

（无）

## 完成的任务
格式：完成日期与时间(GMT+8 timezone) | 任务描述 。 示例： " - 2026-06-20 19:28 | 生成主入口代码"

 - 2026-08-12 | Release 0.1.2-rc.2：全仓版本号 0.1.2-rc.1 → 0.1.2-rc.2（__init__.py / MePage.tsx / ws_master·ws_router app.py / pyproject.toml=0.1.2rc2 / vault_spec×2 / README×2 / user-docs×3 / router-master 部署文档 EVERLINGO_VER）；Chrome 扩展 manifest.json=0.1.2.2、package.json=0.1.2-rc.2、package-lock 经 npm install 同步；发布 tag v0.1.2-rc.2；VERSION_HISTORY.yaml 0.1.2-rc.2 标记 released + 插入 0.1.2-rc.3。注：uv.lock 中 everlingo 包自引用 version 仍为 0.1.2rc1（历次发布均不同步，保持原状）。

 - 2026-08-12 | WS-Master 透传环境变量（代理支持）：ws_master/lifecycle.py 新增 `WS_CONTAINER_ENV_PREFIX` 与 `_collect_container_passthrough_env()`——create 时读取 ws-master 进程 `os.environ`，凡 `WS_CONTAINER_` 前缀变量去掉前缀后注入 ws-container 的 `environment`（`WS_CONTAINER_HTTP_PROXY`→`HTTP_PROXY` 等），透传优先于显式注入的 OPENAI_*/EVERLINGO_* env；测试 tests/test_ws_master_lifecycle.py 新增 3 用例（透传注入/透传覆盖显式/无透传时键集合）；文档 ws-master.md §6.2「透传环境变量」+ §10 不变量、deploy.md §2 ws_master environment 示例；release notes v0.1.2 新增「多用户 · WS-Master」节（14 passed）。

  - 2026-08-11 | Release 0.1.2-rc.1：全仓版本号 0.1.1 → 0.1.2-rc.1（__init__.py / MePage.tsx / ws_master·ws_router app.py / pyproject.toml=0.1.2rc1 / vault_spec×2 / README×2 / user-docs×3 / router-master 部署文档 EVERLINGO_VER）；Chrome 扩展 manifest.json=0.1.2.1、package.json=0.1.2-rc.1、package-lock 经 npm install 同步；VERSION_HISTORY.yaml 标记 released + 插入 0.1.2-rc.2。

 - 2026-08-11 | Memory Vault 版本控制后端文案 i18n（Phase 7）：新增 src/everlingo/i18n/version.py（VERSION_MESSAGES 18 key × zh-CN/en + version_t，与 messages.py 同构）；git.py run_git 三处 GitError/ensure_remote/test_remote 文案、restore.py RestoreResult.message 5 处、committer.py push_now/force_push_now remote_url 未配置、search/server.py /version/* 端点 HTTPException detail（committer 未启动/repo 未初始化/remote_url 未配置/已检出 backup 分支/hard reset）、backup_api.py _unwrap/503/凭证校验全部改 version_t；底层函数透传 interface_language=None（回退 en），indexer 端点 _version_lang()/backup_api _lang() 经 load_resolved_profile() 解析、committer _default_lang() 兜底后台 push；测试 tests/test_i18n_version.py 新增 11 用例 + 修 test_version_restore/test_backup_api 断言（990 passed）。

 - 2026-08-11 | Memory Vault 远端备份 Force Push（git push --force）：git.py push() 新增 force 参数（force=True → --force，默认仍 --force-with-lease）；committer.py push_now(force) + force_push_now()；indexer 新增 POST /version/force-push；SearchClient 新增 version_force_push()；gateway backup_api.py 新增 POST /api/backup/force-push（503 降级）；CLI 新增 mem force-push 子命令；前端 BackupPage.tsx 操作区在「立即推送」右侧新增「强制推送」destructive 按钮（window.confirm 二次确认，拉取恢复改为整行），backup.json（zh-CN/en）加 action_force_push/force_push_confirm；spec vault-version-control.md §4.1/§5/§5.1/§5.2/§6/§11 同步；测试 test_version_git.py 加 --force 与 --force-with-lease 参数断言、test_backup_api.py 加 force-push 成功/503、test_indexer_server_version.py 加 force-push 真实推送集成（46 passed）。

 - 2026-08-11 | Memory Vault 远端备份 P4（HTTPS + PAT 凭证支持 + PAT 掩码）：backup_api.py 放开 https_pat（ALLOWED_METHODS + https_none 共三模式）、BackupConfigBody 新增 pat/pat_changed 字段（omit 或 false → 保留原值；true → 用提交值，空串=清空）、新增 mask_pat（末 4 位掩码，ADR §3.4）并替换 GET config 的恒空 pat；前端 BackupPage.tsx method 下拉加 https_pat + PAT 输入框（patValue/patEdited 跟踪，仅编辑时才提交 pat+pat_changed=true，前端不留存真实 PAT）、types/backup.ts 加 pat_changed、backup.json（zh-CN/en）加 method_https_pat/pat_label/pat_placeholder；测试 test_backup_api.py 更新为掩码断言 + https_pat 接受 + pat_changed 三态 + mask_pat 单测（18 passed）；spec vault-version-control.md §5.1/§5.2/§9/§10/§11 同步标记 P4 已实现。

 - 2026-08-11 | Memory Vault 远端备份 UI（实现 P3 二期）：gateway 新增 backup_api.py（/api/backup/{status,config,snapshot,push,pull,test,reset-hard,log,restore}，经 SearchClient UDS 复用 indexer /version/*，配置读写 everlingo.yaml，P3 仅 ssh/https_none，GET pat 置空掩码）；indexer 新增 /version/{apply-config,test,reset-hard} 端点（热重载 committer / ls-remote 连通探测 / hard reset 强操作）；committer.py 新增 apply_config()/reload_config()（保存配置即热生效，无需重启，init/启停定时线程幂等）；git.py 新增 test_remote()（ls-remote 探测）；前端新增 /console/me/backup 页（web/backup.html + src/backup/BackupPage.tsx：状态卡 / 配置表单 / 测试连接 / 快照 / 推送 / 拉取恢复 / Hard Reset 二次确认 / 历史列表），Vite 多入口加 backup，i18n 加 backup 命名空间，Me 页常驻入口；测试 tests/test_backup_api.py（13）+ test_version_committer.py 补 apply/reload + test_indexer_server_version.py 补 apply-config/test（全量 970 passed）。

 - 2026-08-10 23:01 | 修复失败测试：删除已过时的 system prompt 断言测试（tests/test_mem_writer_agent.py TestWriterSystemPrompt 整类、test_write_kb_item_system_prompt_no_lang_prefix；tests/test_unified_agent.py TestEnvelopeSpecInjection 的 test_includes_envelope_schema_when_provided / test_omits_envelope_schema_when_none 及其 compiled_envelope_spec fixture，同步清理未用 import），与 _build_writer_system_prompt 新增 target_lang/interface_lang 参数及 envelope 节 heading 改为 envelope_spec.md 的现状对齐；events 文件格式对齐源码 `### conversation context` heading：更新 mem_writer_agent 事件写入的断言与 events_spec.md 模板（zh-CN/en）示例，使 test_section_has_all_fields / test_section_omits_conversation_context_when_none 通过（全量 952 passed）。

 - 2026-08-10 | Memory Vault 版本控制加固「启动强制统一本地分支名」：git.py 新增 rename_current_branch（git branch -M，幂等，失败仅告警不阻塞）+ init_repo 兼容 git<2.28（-b 失败降级 init + branch -M main）；Committer.start() init 后强制 rename 本地分支=配置 branch；测试补充 legacy-master repo 收敛到 main、rename 幂等/非 repo/降级路径；impl-spec vault-version-control.md §2.3/§4.1 补「强制统一分支名」说明。
 - 2026-08-10 | Memory Vault 版本控制与远端备份（实现 P1+2）：新增 src/everlingo/mem/vault/version/{git,committer,ssh_key,restore}.py（subprocess git CLI 封装，统一 safe.directory；定时器 commit 去抖 + push 间隔 + initial/final commit 兜底；临时私钥 + GIT_SSH_COMMAND；restore 冲突打 backup 分支）；models.py / setting.py 加 git_backup 配置段与 load/save_git_backup；search/server.py AppState 持 Committer 随 lifespan 启停、新增 /version/{status,commit,push,pull,log,restore} 端点；client.py / cli.py / main.py 加对应客户端方法与 mem snapshot/push/pull/restore/log/vstatus 子命令；tools/user_doc.py 停 USER.md.bak（改 git 回溯，DOMAIN.md/chat-agent-tools-spec.md 已在上一条更新）；mcp_server reset_vault 覆盖 spec 前 snapshot 安全网；测试 tests/test_version_{git,committer,ssh_key,restore}.py + tests/test_indexer_server_version.py + test_user_doc.py 更新；release notes 记 docs/release-notes/v0.1.2/。
 - 2026-08-10 | Memory Vault 版本控制与远端备份（设计定稿 + 文档）：覆盖 docs/ADR/20260810-vault-version-control.md（原 SaaS GitHub App + Contents API 方案标 superseded，新定本地 ws-container 方案：committer 在 indexer 进程、通用 git remote + git CLI、ssh/https_pat/https_none 三凭证、ws-master 不参与、everlingo.yaml 0600 兜底）；新写 docs/impl-spec/worksplace/vault-version-control.md（git repo 管理 / committer / git 操作封装 / indexer HTTP 端点 / CLI / 停 USER.md.bak / reset_vault 安全网 / 测试 / 分期 P1+2,P3,P4）；everlingo.example.yaml + user-docs/reference/configuration.md 加 git_backup 段；DOMAIN.md + chat-agent-tools-spec.md 移除 USER.md.bak 提及；deploy/deps-base/Dockerfile runtime stage 加 git openssh-client ca-certificates；deploy/ws-container/ws-container-spec.md 加「系统依赖」小节（git/openssh-client/ca-certificates 经 deps-base 共享）。TASKS + 后续实现排期见 vault-version-control.md §7。

