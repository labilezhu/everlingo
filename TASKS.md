# Tasks

## 计划的任务

（无）

## 完成的任务
格式：完成日期与时间(GMT+8 timezone) | 任务描述 。 示例： " - 2026-06-20 19:28 | 生成主入口代码"

 - 2026-08-17 | 聊天图片写入笔记（Chat → Vault Image）实现：打通 Chat Agent 把聊天中的图片复制到 vault 并在 markdown 中引用的闭环。image_store.py `save_vault_image` 新增可选 kwarg `src_resource_sha256`（None 保留旧行为：从 stem 提取并校验 64-hex；显式传入：跳过 stem 校验、stem 可为英文 slug，仅对传入值本身做 64-hex 校验防元数据注入）+ 新增模块级 `slugify(text)`（小写→非 [a-z0-9] 转 '-'→折叠→限长 40→空串回退 "image"）；新增 `tools/image_vault_copy.py` `make_copy_session_image_tool(image_store, target_lang)`（`copy_session_image_to_vault(src_resource_sha256, md_file_path, slug_hint)` 工具：`image_store.read_bytes` 取回已预处理字节 → slugify+`-{src_sha[:8]}` 拼 stem → 按 vault-editor 约定写 `{md_dir}/{mdname}.assets/{stem}.{ext}` → `save_vault_image`（显式 src_sha）→ 返回 `markdown_relative_path`，失败返回 ok=false 不抛异常，bytes 不可取/ValueError 均降级）；agent.py `_refresh_agent_if_needed` 在 `channel.supported_image` 时注入该工具，system prompt 「笔记编辑」节新增「嵌入聊天图片（编辑流程）」（仅 supports_image 时注入，先复制再 memory_writer_action）；mem_writer_agent.py `_write_kb_item_async` 对 MCP tools 追加该工具（bind entry.lang），Writer system prompt 新增「嵌入聊天图片（create 流程）」节。测试：test_image_store.py 新增 `test_explicit_src_sha_with_slug_stem`/`test_explicit_src_sha_invalid_value_raises` + `TestSlugify`(7)；新增 test_image_vault_copy.py（6 例：复制+相对路径+EXIF/tEXt 溯源/根目录 md/字节不可取/幂等/slug 空回退/JPEG）；test_mem_writer_agent.py 新增 `test_process_batch_injects_copy_image_tool`。文档：新增 ADR docs/ADR/20260817-save-image-from-chat-to-note.md（决策 1-5 + 待回填 spec 片段），v0.1.2 release notes 中/英新增「聊天图片写入笔记」节。验证：test_image_store + test_image_vault_copy 41 passed；test_mem_writer_agent 38 passed；test_main_agent 16 passed；test_tools+vision_tool+vault_editor_api+web_acceptor 103 passed。

 - 2026-08-17 | Vault Editor 支持粘贴图片 + 修复 iOS 只能拍照不能选相册：新增 `pasteImage.ts`（`extractImageFile` 从剪贴板取首个受支持类型图片，jpeg/png/webp 与 `<input accept>` 对齐，gif 等不支持类型返回 null 不拦截）；`EditorApp` 在编辑区 `<main>` 以 **capture 阶段**监听 `paste`（`handlePaste`，用 ref 读最新 currentPath/handlePickImage 保证监听器只绑定一次），命中图片 → `preventDefault()`（先于 ProseMirror/CodeMirror 自身处理避免重复插入）→ 复用 `handlePickImage` 同一上传/插入流程，未打开文件时提示「请先保存文件」；同时**移除** `<input type="file">` 的 `capture="environment"` 属性——iOS Safari 下 `capture` 会强制只打开相机隐藏相册，去掉后系统弹出「拍照/从相册选择」面板。测试：新增 `pasteImage.test.ts`（6 例：png 命中/多 item 取首个/纯文本/gif/空 clipboardData/getAsFile null）；扩展 `EditorApp.image.test.tsx`（4 例：粘贴走同一上传插入流程、纯文本不拦截、未打开文件提示请先保存、beforeEach 重置 history URL 修复 jsdom 跨用例污染导致的启动参数 initPath 泄漏）。文档：vault-editor.md「图片插入」节补粘贴说明 + 移除 capture 描述；ADR 决策 8、§4 文件表、§5 Phase 2 同步；v0.1.2 release notes 中/英同步（粘贴图片 + 移动端拍照/相册可选）。验证：前端 vitest 60 passed + `npx tsc --noEmit` 通过。

 - 2026-08-17 | Markdown 图片插入 Phase 1（后端闭环）实现：image_store.py 新增模块级 save_vault_image(lang, vault_rel_path, data, mime_type)（无状态、按路径幂等）——复用 ALLOWED_MIME/sha256_of_bytes/preprocess_image（EXIF 校正→strip→超限缩放），校验 lang 名合法性与 vault 内逃逸（resolve + is_relative_to）、末段 stem 等于重算 src_sha256（Phase 2 改为信任 stem 格式校验），best-effort 写自有溯源元数据（JPEG EXIF UserComment tag 0x9286、PNG tEXt，仅自有键不影响隐私 strip），落盘 lang_vault_dir/vault_rel_path（父目录自动建、存在即跳过），storage_key=memory://languages/{lang}/vault/{vault_rel_path}；vault_editor_api.py 末尾新增 PUT/GET /api/vault/raw/{lang}/{vault_rel_path:path}（纯本地文件系统不走 MCP）：GET 按扩展名定 Content-Type（png/jpeg/webp→image/*、md/txt/json/yaml/csv→text/plain、其它 octet-stream）+ immutable 缓存头，PUT multipart 校验（415 MIME/400 空文件/400 sha mismatch/400 path escape/400 invalid image data/400 非法 lang），复用 save_vault_image。测试：test_image_store.py 新增 TestSaveVaultImage（11 例：落盘/storage_key/幂等 mtime/大图缩放/EXIF 方向/JPEG EXIF/PNG tEXt/逃逸/非法 lang/sha mismatch/假图）、test_vault_editor_api.py 新增 TestRawUpload(8) + TestRawGet(8)。验证：test_image_store + vault_editor_api + web_acceptor 113 passed；curl 手工验收（PUT 上传回 image 对象 → GET 取回字节 hash 与 saved_resource_sha256 一致）。
- 2026-08-17 | Markdown 图片插入 Phase 3（文档回填）完成：vault-editor.md 在「编辑区（Milkdown）」后新增「### 图片插入」节（按钮/accept/capture、前置 currentPath 校验、上传路径默认规则 {md_dir}/{mdname}.assets/{sha}.{ext}、scale 前算 sha、插入形态 WYSIWYG 绝对/ Source 相对、toDisplay/toRelative 改写规则与已知限制、上传状态、MCP 无关）并删除「不在本 spec 范围」图片行；REST 端点表补 PUT/GET /api/vault/raw/{lang}/{vault_rel_path:path} 两行（不经 MCP 进程内直调）；vault-mcp-spec-tools.yaml 核对 18 个工具（图片上传/取回不经 MCP 无需新增工具）并加说明注释；image-store-spec.md §3.1 交叉引用由「Phase 2 回填」改为指向已完成的 vault-editor.md；ADR 状态更新为 Accepted、Phase 3 各步骤标 ✅。Release Notes v0.1.2 中/英新增「Vault 图片 · 前端」小节并同步后端 stem 信任语义。
 - 2026-08-17 | Markdown 图片插入 Phase 2（前端 + 后端契约微调）实现：
 - 后端契约微调：`save_vault_image` 的 stem 校验由「重算收到的字节比对」改为「仅校验 64 位 hex 并信任为前端 scale 前原始 `src_resource_sha256`」（前端可能缩放，字节 sha ≠ 原始 sha）；更新 docstring、`tests/test_image_store.py`（`test_sha_mismatch_raises` 语义 + 新增 `test_scaled_bytes_under_original_sha_accepted`）与 `tests/test_vault_editor_api.py`（`TestRawUpload` 新增 scaled-bytes 用例）；同步 ADR 决策 4/5、§3 错误表、§4 文件表、§5 Phase 2、§6 风险与 `image-store-spec.md` §3.1 校验描述。
 - 前端：`types/vault.ts` 新增 `ImageAsset`；`vaultApi.ts` 新增 `uploadImage`/`assetUrl`；新增 `imageLinks.ts`（`toDisplay`/`toRelative` 相对↔绝对改写 + `buildUploadPath`/`extFromMime`/`mdNameFromPath`/`relPath`/`normalizeVaultPath` 路径工具）；新增 `imageScale.ts`（`scaleImageIfNeeded` 必要时 canvas 等比缩放 ≤1920×1200、失败回退原图 + `shouldScale`）；`MilkdownEditor` WYSIWYG 用 `toDisplay` 渲染、`markdownUpdated` 先 `toRelative`，经 `insertImageRef` 暴露光标插入 image 节点（src 绝对 URL）；`SourceEditor` 经 `insertImageRef` 光标插入相对 `![alt](rel)` 文本；`EditorApp` sub-header 新增「插入图片」按钮 + 隐藏 file input（accept 三 MIME、移动端 `capture="environment"`）+ `handlePickImage`（**scale 前**算原始 sha → `buildUploadPath` → `scaleImageIfNeeded` → `uploadImage` → 插入，alt=md 文件名）+ `uploading` 状态与错误条；i18n `editor.json`（zh-CN/en）新增 `insert_image`/`insert_image_uploading`/`insert_image_failed`/`insert_image_save_first`。
 - 测试：`imageLinks.test.ts`（20 例：往返不变式、`../`、vault 根 `/`、外链/data/跨 lang 不动、title 保留、`buildUploadPath` 嵌套与根目录等）；`imageScale.test.ts`（6 例：`shouldScale` 边界、`createImageBitmap` 缺失/失败、超限缩放、限内关闭 bitmap、ctx 不可用）；`EditorApp.image.test.tsx`（3 例：上传成功→`uploadImage` 参数与插入回调、未打开文件不渲染按钮、上传失败显示错误且不插入；mock 重型子组件 + matchMedia polyfill）。
 - 验证：后端 `test_image_store` + `test_vault_editor_api` + `test_web_acceptor` 115 passed；前端 `npx tsc --noEmit` 通过 + vitest 51 passed。

 - 2026-08-15 | 图片学习能力正式设计文档落盘：新增 docs/impl-spec/vision/ 目录，把已实现的 Image Store 与 Vision Service 相关内容从 ADR（20260812-image-chat.md）沉淀为正式 Implementation Spec——image-store-spec.md（ImageAsset/MessageAttachment 数据模型、存储位置 storage_key↔物理路径、前端 PUT /api/session/{session_id}/images/{src_sha} 上传端点契约（404/415/400/幂等）、Pillow 上传预处理（EXIF 校正→strip→等比缩放 1920×1200→重算 saved_sha/width/height）、read_bytes、§32 资源限制、§29 错误映射、存储生命周期）；vision-service-spec.md（§10 职责边界、ImageInput/ImageAnalysis/VisionPurpose 数据模型、VisionService Protocol + OpenRouterVisionService、配置 vision_model/get_vision_llm_config/create_vision_llm、§21/§23 缓存与 in_flight 并发防护（注明 cache key 额外含 purpose 的偏差）、§22 Eager Warm + Tool Fetch 混合模式、analyze_image 工具与 supported_image 门控、§29 错误码映射、Phase2/3 Done 状态）。两文档均标注 Implemented 并交叉引用 ADR 与源码路径。另同步 everlingo.example.yaml 补 sys_setting.vision_model 字段注释。

 - 2026-08-15 | 图片学习能力 Phase 2 实现（Vision Service + 缓存/并发 + Eager Warm）+ Phase 3 工具接入：新增 Pillow 依赖（pyproject.toml）用于缩放/EXIF 校正/strip metadata；image/models.py 新增 VisionPurpose(str Enum: ocr/exercise/document/learning_content/general)、ImageInput（src_resource_sha256 引用）、ImageAnalysis（§9：content_type/language/text/structured_content:dict 宽松/ knowledge_points，含 src_resource_sha256/model desc）；image_store.py 改造 save() 引入 preprocess_image（EXIF 方向校正→strip 元数据→超 1920x1200 按比例缩放→重算 saved_resource_sha256、填 width/height）+ 新增 read_bytes()（storage_key→物理路径回读）；新增 image/vision_service.py（VisionService Protocol + OpenRouterVisionService：base64 data URI→ChatOpenAI vision，purpose 专化 system prompt，JSON 解析；persistent_cache LRU/TTL 7 天 + in_flight Future 合并并发防护 §21/§23；错误映射 ADR §29 VisionServiceError→VISION_MODEL_UNAVAILABLE/VISION_OUTPUT_INVALID；PROMPT_VERSION 参与 cache key）；config.py 新增 get_vision_llm_config()（setting.sys_setting.vision_model / env VISION_MODEL，默认 xiaomi/mimo-v2.5）+ llm.py create_vision_llm()（temperature=0, request_timeout=120）；web_acceptor.py 上传成功后 asyncio.create_task(_eager_warm) 异步预热（失败静默 §14/§22）；新增 tools/vision_tool.py make_vision_tool()（analyze_image 工具，仿 memory_writer_action，Vision 失败返回友好降级文案而非抛异常 §29）；channel.py/web_channel.py 新增 supported_image 能力位（web=True）；agent.py 图片能力 system prompt 段 + supported_image 时注册 analyze_image 工具。测试：重写 tests/test_image_store.py（真实 Pillow 图片：缩放/EXIF 方向/strip 元数据/幂等，13 例）、新增 tests/test_vision_service.py（缓存命中/in_flight 并发合并为一次调用/错误映射，fake LLM）与 tests/test_vision_tool.py（JSON 返回/失败降级）；修复 WebAcceptor 上传测试使用假图字节被 Pillow 拒绝（改真实图）。验证：test_image_store+vision 三文件 24 passed；agent/gateway/channel 相关套件 92 passed。

 - 2026-08-14 | 图片学习能力 ADR 设计修订（HLD）：将"模式 A/B 二选一"改为**混合模式**——上传即 Eager Warm（PUT /api/v1/images/{sha256} 存储后 fire-and-forget 异步预热 Vision 缓存，不阻塞 200）+ Agent 经 `analyze_image(src_resource_sha256)` 工具 cache-first 取用，ImageAnalysis 作为 ToolMessage 进入消息历史（取代原 XML 注入设想）。新增 §23 并发分析防护（单进程内存 `in_flight` Future 合并，保证同一 sha256 至多一次 Vision 调用）；§12 明确工具为 Agent 取用图片结果的唯一路径；§13 改写 Agent 输入结构；§14 补 Eager Warm 步骤；§21 明确全局共享缓存（跨用户，仅文本无隐私风险）且 Eager/Tool 共用；§29 补错误→HTTP 映射与 Agent 工具降级；§32 补 MVP 资源限制具体值（10MB / 1 图 / 1920x1200 / 先 EXIF 校正再 strip）与 vision 配额；§34 重写实现顺序为 Phase1~4（含图片 Memory 衔接）。同步更新 envelope_spec.md（`chat.attachments` 字段定义 + 附件非空不触发"延续话题"例外）与 web-chatbot.md（新增「图片上传」UI 小节，对齐需求 §12）。决策：Vision 模式=混合、图片绑 Session、缓存全局共享、不做 crop、purpose 固定 general、上传用 PUT。

 - 2026-08-14 | 图片学习能力 Phase 1 文档定稿：ADR §14 上传端点由示例路径改为实际 `PUT /api/session/{session_id}/images/{src_resource_sha256}`，新增「存储位置」小节（落盘 `{workspace}/sessions/{session_id}/images/`，复用 workspace.current_workspace()，逻辑键 storage_key=session://{session_id}/{saved_resource_sha256}）；§34 Phase 1 重写为「前后端最小闭环（不含 Vision）」含存储路径/端点/单图限制/Phase 1 不缩放(Pillow 延 Phase 2)；web-chatbot.md 图片上传小节同步端点与「Phase 1 用户气泡 URL.createObjectURL 渲染、不新增 GET 回取」。确认 xiaomi/mimo-v2.5 为 OpenRouter 真实 id。

 - 2026-08-14 | 图片学习能力 Phase 1 实现（前后端最小闭环）：后端新增 src/everlingo/image/{models.py,image_store.py}（ImageAsset/MessageAttachment、ImageStore 本地文件系统 `{workspace}/sessions/{session_id}/images/{sha256}.{ext}`、sha256_of_bytes、ALLOWED_MIME={jpeg,png,webp}、单例 image_store、save 幂等）；gateway 新增 PUT /api/session/{session_id}/images/{src_resource_sha256}（multipart，404 未知会话 / 415 MIME 不支持 / 400 sha 不匹配或空文件）与消息端点单图限制（chat.attachments>1→400）；envelope 加 AttachmentPart + ChatPart.attachments。前端：types/chat.ts 加 MessageAttachment/ImageAsset/Message.imageUrl、sseClient.ts buildEnvelope 支持 attachments + uploadImage（FormData file 字段）、ChatInput.tsx 重写（[+] ImagePlus 按钮 / 粘贴截图 / crypto.subtle computeSha256 / 预览删除 / 上传中状态）、ChatWindow.tsx handleSend(text,attachments,imageUrl?)、MessageBubble.tsx 渲染 user 气泡 imageUrl（URL.createObjectURL）、locale（zh-CN/en）加 attach_image/remove_image/uploading/upload_failed。测试：新增 tests/test_image_store.py（14）、test_web_acceptor.py TestUploadImage（成功/404/415/400/幂等）、test_envelope.py TestChatAttachments；修复新测试 init_workspace_dir(tmp_path) 泄漏全局 workspace 状态导致 test_setting/test_wechat_channel 全量失败的污染（teardown 复位 init_workspace_dir(None)）。验证：全量 pytest 1011 passed；web tsc --noEmit 通过 + vitest 17 passed。Release notes v0.1.2 追加「聊天图片 · 一期」节（zh-CN/en）。

 - 2026-08-14 | 修复 Web Chatbot 选图后前端崩溃（crypto.subtle 不可用）：HTTP（非 HTTPS/localhost 安全上下文）下 `crypto.subtle` 为 undefined，ChatInput.computeSha256 调用 `crypto.subtle.digest` 抛 "Cannot read properties of undefined (reading 'digest')"。新增 web/src/lib/sha256.ts：sha256Hex 优先用原生 crypto.subtle，不可用时回退纯 JS SHA-256 实现（sha256Bytes，NIST 向量验证 + 与原生跨 8 种长度比对一致，注意 JS 位运算结果为有符号 32 位需 >>>0 再 toHex）；ChatInput.computeSha256 改为委托 sha256Hex。测试：web/src/lib/sha256.test.ts 新增 3 用例（NIST 向量 / 跨长度对拍 / 委托原生）；vitest 20 passed + tsc --noEmit 通过。

 - 2026-08-14 | 修复发图后输入框/Send 按钮持续 disable：ChatInput.handleSubmit 上传图片成功路径漏掉 `setUploading(false)`（仅在 catch 里复位），而 Textarea/Send 按钮禁用条件是 `disabled={disabled || uploading}`，导致成功发图后 uploading 恒为 true、UI 永久禁用（纯文本发送不受影响）。修复：上传成功（onSend 前）补 `setUploading(false)`。测试：新增 web/src/components/ChatInput.test.tsx 2 用例（mock uploadImage + sha256Hex，成功路径 Send 恢复可用且 onSend 调用一次；失败路径显示 upload_failed 且不调 onSend、Send 恢复可用）；vitest 22 passed + tsc --noEmit 通过。
