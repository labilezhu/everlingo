# LLM Provider 配置
# 支持 OpenAI Chat Completions endpoint (通过 LangChain)
# 也可在 everlingo.example.yaml 的 sys_setting 中配置，优先级高于环境变量
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=
# Embedding 模型（可选，无默认值）。空值时 create_ai_embedding() 抛错
OPENAI_EMBEDDING_MODEL=
